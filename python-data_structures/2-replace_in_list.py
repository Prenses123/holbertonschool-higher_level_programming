#!/usr/bin/python3
def replace_in_list(my_list, idx, element):
    if idx < 0 or idx >= len(replace_in_list):
        return replace_in_list
    replace_in_list[idx] = element
    return replace_in_list
